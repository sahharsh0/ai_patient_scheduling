# Copy this file to .env.local and adjust for your environment.
# Never commit a real .env.local file.

# Base URL of the backend API. For production, point this at your deployed
# backend (e.g. a Railway/Render/Fly.io URL), not localhost.
NEXT_PUBLIC_API_URL=http://localhost:8000
